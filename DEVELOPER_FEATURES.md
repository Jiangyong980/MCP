# 禅道 MCP 功能分类与优先级

## 概述

本文档明确区分**开发者相关功能**与**非开发者功能**，并制定实现优先级。

---

## 功能分类

### ✅ 一级优先级：开发者核心工具（已完全实现）

这些功能是开发者日常工作必需的，已全部实现。

#### 项目框架管理

| 模块 | 工具 | 覆盖 | 用途 |
|------|------|------|------|
| **Programs (项目集)** | `list_programs`, `get_program`, `create_program`, `update_program`, `delete_program` | 100% | 查看和管理项目集结构 |
| **Projects (项目)** | `list_projects`, `get_project`, `create_project`, `update_project`, `delete_project`, `get_project_executions` | 100% | 项目管理和迭代查询 |

#### 迭代规划

| 模块 | 工具 | 覆盖 | 用途 |
|------|------|------|------|
| **Executions (迭代)** | `list_executions`, `get_execution`, `update_execution`, `delete_execution`, `create_execution`, `get_execution_tasks`, `get_execution_stories` | 100% | 迭代规划和任务管理 |

#### 需求与任务

| 模块 | 工具 | 覆盖 | 用途 |
|------|------|------|------|
| **Stories (需求)** | `get_story`, `create_story`, `update_story`, `delete_story`, `change_story` | 100% | 需求追踪 |
| **Tasks (任务)** | `get_task`, `create_task`, `update_task`, `delete_task` | 100% | 开发任务分配和进度 |

#### 质量保证

| 模块 | 工具 | 覆盖 | 用途 |
|------|------|------|------|
| **Bugs (Bug)** | `get_bug`, `create_bug`, `update_bug`, `delete_bug` | 100% | 缺陷追踪和修复 |

#### 团队协作

| 模块 | 工具 | 覆盖 | 用途 |
|------|------|------|------|
| **Users (用户)** | `list_users`, `get_user`, `get_my_info` | 100% | 查看团队成员信息 |

---

### ✅ 二级优先级：开发者信息查询（已部分实现）

这些功能帮助开发者了解产品信息，部分已实现。

#### 产品信息

| 模块 | 工具 | 覆盖 | 状态 | 用途 |
|------|------|------|------|------|
| **Products (产品)** | `list_products`, `get_product`, `create_product`, `update_product`, `delete_product`, `get_product_stories`, `get_product_bugs` | 100% | ✅ 已实现 | 产品和相关需求/Bug 查询 |

---

## 功能缺失情况

### 开发者相关的缺失功能

目前开发者相关功能已全部实现，暂无缺失。

**可选补充**（优化用户体验）：
- Product 版本和发布列表（获取产品的所有发布版本）
- Project 版本和发布列表（获取项目的所有发布版本）
- User 创建/修改功能（当前仅支持查询）

---

## 非开发者功能（暂不实现）

以下功能主要面向项目经理、测试人员或支持团队，在后续版本中实现：

### 🔮 测试相关模块（后续版本）

| 模块 | 工具数 | 主要用户 | 优先级 |
|------|--------|---------|--------|
| **TestCase (测试用例)** | 3 | 测试人员 | 低 |
| **TestTask (测试任务)** | 2 | 测试人员 | 低 |

**缺失的接口**：
- `GET /testcases` - 获取测试用例列表
- `GET /testcases/:id` - 获取测试用例详情
- `POST /testcases` - 创建测试用例
- `PUT /testcases/:id` - 修改测试用例
- `DELETE /testcases/:id` - 删除测试用例
- `GET /testtasks` - 获取测试任务列表
- `GET /testtasks/:id` - 获取测试任务详情

### 🔮 反馈与支持（后续版本）

| 模块 | 工具数 | 主要用户 | 优先级 |
|------|--------|---------|--------|
| **Feedback (反馈)** | 7 | 产品经理 | 低 |
| **Ticket (工单)** | 5 | 支持团队 | 低 |

**缺失的接口**：
- 反馈相关：查询、创建、修改、删除、指派、关闭反馈
- 工单相关：查询、创建、修改、删除工单

### 🔮 版本与发布（后续版本）

| 模块 | 工具数 | 主要用户 | 优先级 |
|------|--------|---------|--------|
| **Build/Release (构建/发布)** | 3-5 | 发布管理 | 低 |
| **Plan (计划)** | 5+ | 项目经理 | 低 |

**缺失的接口**：
- Build：创建、获取、修改、删除构建版本
- Release/Plan：产品计划、版本管理、计划与需求/Bug 的关联

### 🔮 认证与令牌（基础设施）

| 模块 | 工具数 | 用途 |
|------|--------|------|
| **Token (令牌)** | 1 | API 认证 |

**状态**：
- 当前在客户端代码中已实现 `_authenticate()` 方法，自动处理认证
- 暂无需为用户暴露 Token 工具

---

## 工作流与优先级映射

### 开发人员的日常工作流（完全支持）

```
早上                          下午
├─ 查看迭代任务              ├─ 创建 Bug
│  └─ get_execution_tasks()   │  └─ create_bug()
├─ 查看分配给我的任务        └─ 修改任务状态
│  └─ list_tasks() / get_task() └─ update_task()
├─ 查看需要实现的需求
│  └─ get_story()             晚上
└─ 跟踪 Bug 修复进度          └─ 汇报完成情况
   └─ get_bug()                  └─ get_project_executions()
```

**支持程度：✅ 100% 支持**

### 项目经理的工作（暂不支持）

```
├─ 创建测试计划              ❌ 不支持
├─ 分配测试任务              ❌ 不支持
├─ 管理版本和发布            ❌ 不支持
└─ 分析反馈                  ❌ 不支持
```

### 测试人员的工作（暂不支持）

```
├─ 创建和维护测试用例        ❌ 不支持
├─ 执行测试任务              ❌ 不支持
└─ 报告测试结果              ❌ 不支持
```

---

## 如何扩展功能

### 添加新的开发者工具示例

如果需要添加新的开发者相关功能，参考以下步骤：

#### 1. 在 `client.py` 中添加方法

```python
# 例如：获取产品的所有发布版本
def get_product_releases(self, product_id: int) -> Dict:
    """Get releases for a product"""
    return self._request("GET", f"/products/{product_id}/releases")
```

#### 2. 在 `server.py` 中注册工具

```python
@server.list_tools()
async def list_tools() -> list[Tool]:
    return [
        # ... 其他工具 ...
        Tool(
            name="get_product_releases",
            description="Get all releases for a product",
            inputSchema={
                "type": "object",
                "properties": {
                    "product_id": {
                        "type": "integer",
                        "description": "Product ID"
                    }
                },
                "required": ["product_id"]
            }
        ),
    ]
```

#### 3. 在 `server.py` 中添加处理器

```python
@server.call_tool()
async def call_tool(name: str, arguments: dict) -> list[TextContent]:
    if name == "get_product_releases":
        client = get_client()
        result = client.get_product_releases(arguments["product_id"])
        return [TextContent(type="text", text=json.dumps(result, ensure_ascii=False, indent=2))]
```

#### 4. 在手册中记录

在 `禅道MCP使用手册.md` 中的相应模块中添加工具文档。

---

## 总结

- **当前状态**：38 个工具，完全覆盖开发者核心需求
- **缺失工具**：主要是项目经理、测试、支持相关的工具
- **建议使用**：开发人员可以放心使用，项目经理/测试人员暂需等待后续版本

---

**最后更新**: 2026-01-30
